seed: 42
data_root: data/raw/birdclef-2026
processed_dir: data/processed
output_dir: outputs/perch_probe

audio:
  sample_rate: 32000
  duration: 5.0

perch:
  embedding_dim: 1536
  batch_size: 16
  model_dir:
  embeddings_path: outputs/perch_probe/train_embeddings.npz

probe:
  hidden_dim: 512
  dropout: 0.25
  epochs: 15
  batch_size: 128
  lr: 0.001
